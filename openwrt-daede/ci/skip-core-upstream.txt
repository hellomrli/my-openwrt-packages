# Known-incompatible daeuniverse/dae main commits.
#
# The auto-bump core lane skips (treats as "no change") any full 40-char SHA
# listed here, so a breaking upstream rework stops failing the schedule every
# 6 hours until the incompatibility is resolved.
#
# Format: one full commit SHA per line; blank lines and # comments are ignored.
# To resume tracking a commit, remove its line.

# feat: kdae full sync — datapath, control plane and behaviour rework (#1099).
# Rewrites the control-plane/datapath API: control.NewControlPlane ->
# NewControlPlaneWithContextOptions (bpf type changed), SnapshotRuntimeStats /
# UdpEndpointPool.Count removed, netutils.FallbackDns removed, dialer.NewFromLink
# -> NewFromLinkContext, config marshaller/function APIs changed. dae-wing is
# pinned at dc50308 (2026-05) and daed at 671e65d (2026-07); neither is
# compatible yet. Re-enable after 010-dns-response-ttl.patch is re-ported and
# dae-wing is adapted to the new core API.
1ec85feddc721088ecdda73015bd78f652926b39
